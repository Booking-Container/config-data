server:
  port: 8082

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://postgres-user:5432/user_db
    username: user_db
    password: "H@7zP!4xQw9^rS2mJf8L"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

# RabbitMQ Configuration
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: bc.rabbit@msg.com
    password: "D8@zP!4xQw9^rS2mKf8J"
    template:
      retry:
        enabled: true
        initial-interval: 2s
        max-attempts: 3
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 2s
          max-attempts: 3

# Brevo / Email configuration
brevo:
  api-key: ${BREVO_API_KEY}  # Never hardcode production API key
  sender:
    email: no-reply@booking-container.com
    name: Booking Container

app:
  activation:
    token:
      expiry-hours: 24
  frontend:
    url: ${FRONTEND_URL:https://booking-container.com}

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://discovery-service:8761/eureka/
