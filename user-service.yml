# In Config Server - user-service.yml
server:
  port: 8082  # Port where the user service will run

spring:
  application:
    name: user-service  # Service name

  datasource:
    url: jdbc:postgresql://postgres-user:5432/user_db  # PostgreSQL URL for user service
    username: user_db
    password: "H@7zP!4xQw9^rS2mJf8L"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

# EMAIL CONFIGURATION
brevo:
  api-key: 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
  sender:
    email: no-reply@booking-container.com
    name: Booking Container

  # ADD RABBITMQ CONFIGURATION HERE
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: bc.rabbit@msg.com
    password: "D8@zP!4xQw9^rS2mKf8J"
    template:
      retry:
        enabled: true
        initial-interval: 2s
        max-attempts: 3
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 2s
          max-attempts: 3
          
# ADD APPLICATION-SPECIFIC CONFIG
app:
  activation:
    token:
      expiry-hours: 24
  email:
    from: ${EMAIL_FROM:no-reply@booking-container.com}
    activation-subject: "Activate Your Booking Container Account"
  frontend:
    url: ${FRONTEND_URL:https://booking-container.com}

eureka:
  client:
    register-with-eureka: true  # Register the user service with Eureka
    fetch-registry: true
    service-url:
      defaultZone: http://discovery-service:8761/eureka/  # Eureka server URI