# In Config Server - user-service.yml
server:
  port: 8082  # Port where the user service will run

spring:
  application:
    name: user-service  # Service name

  datasource:
    url: jdbc:postgresql://postgres-user:5432/user_db  # PostgreSQL URL for user service
    username: user_db
    password: "H@7zP!4xQw9^rS2mJf8L"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  # ADD EMAIL CONFIGURATION HERE
  mail:
    host: smtp.gmail.com
    port: 587
    username: devhicham00@gmail.com
    password: xvrqfdgwqejuwuwm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        debug: false

  # ADD RABBITMQ CONFIGURATION HERE
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: bc.rabbit@msg.com
    password: "D8@zP!4xQw9^rS2mKf8J"
    template:
      retry:
        enabled: true
        initial-interval: 2s
        max-attempts: 3
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 2s
          max-attempts: 3
# ADD APPLICATION-SPECIFIC CONFIG
app:
  activation:
    token:
      expiry-hours: 24
  email:
    from: ${EMAIL_FROM:noreply@bc.com}
    activation-subject: "Activate Your Booking Container Account"
  frontend:
    url: ${FRONTEND_URL:https://192.168.151.122:7000}

eureka:
  client:
    register-with-eureka: true  # Register the user service with Eureka
    fetch-registry: true
    service-url:
      defaultZone: http://discovery-service:8761/eureka/  # Eureka server URI